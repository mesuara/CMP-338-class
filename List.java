class List{
  // List ADT
  // Insert
  // remove
  // search
  // isEmpty
  public void insertBefore (int x){
    
    //prev.next = new Node(x,  previous.next);
         
  }

  public void insertAfter(int x){
    //prev = prev.next;
    // prev.next = new Node(x, previous.next)
    //             insertBefore(x);
  }
}